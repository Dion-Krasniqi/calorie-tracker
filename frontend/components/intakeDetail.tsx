import { View, Text } from 'react-native'
import React from 'react'

const IntakeDetail = (props: IntakeCurrent) => {
    console.log(props);
  return (
    
    <View>
      <Text className='text-white'> calories consumed</Text>
    </View>
  )
}

export default IntakeDetail